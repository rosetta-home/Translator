import { h, Component } from 'preact';
import { Link } from 'preact';
import { Card, Button } from 'preact-mdl';

export default class HomePage extends Component {
	login = () => {

	};
	render() {
		return (
			<div>
			</div>
		);
	}
}
